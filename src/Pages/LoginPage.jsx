import React, { memo } from 'react';
import { Login } from '../Components';

const LoginPage = () => {
    return (
        <div>
            <Login />
        </div>
    );
}

export default memo(LoginPage);
