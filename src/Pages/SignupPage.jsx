import React, { memo } from 'react';
import { Signup } from '../Components';

const SignupPage = () => {
    return (
        <div>
            <Signup />
        </div>
    );
}

export default memo(SignupPage);
